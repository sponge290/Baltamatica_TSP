{
  "name": "baltamatica-tsp",
  "version": "0.0.0",
  "private": true,
  "engines": {
    "node": "20.x"
  },
  "scripts": {
    "dev": "npm --prefix project/frontend run dev",
    "build": "npm --prefix project/frontend run build",
    "preview": "npm --prefix project/frontend run preview"
  }
}

